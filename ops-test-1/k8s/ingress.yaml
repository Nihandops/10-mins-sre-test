apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sre-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sre-service
            port:
              number: 80
